import React from "react";

const NotFound = (props) => {
  return (
    <div>
      <p>NOT FOUND</p>
      <p>
        {/* <button onClick={() => props.history.goBack()}>Back</button> */}
      </p>
    </div>
  );
};

export { NotFound };
